export function AppFooter() {
  return (
    <footer className="mt-auto border-t border-border py-8 text-center text-sm text-muted-foreground">
      <p>
        Billing platform — orgs, companies, teams, multi-gateway payments, and
        Stripe-like APIs.
      </p>
    </footer>
  );
}
